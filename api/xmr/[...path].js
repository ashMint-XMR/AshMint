import { handler } from "../../server.mjs";

// Vercel function adapter. Production claims remain fail-closed unless all
// required infrastructure and reviewed verifier configuration is supplied.
export default handler;
